{
  "name": "liora-media",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "install:all": "npm ci && npm --prefix frontend ci",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
